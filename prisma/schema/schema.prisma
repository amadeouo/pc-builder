generator client {
  provider = "prisma-client"
  output   = "../../src/lib/prisma"
}

datasource db {
  provider = "postgresql"
}

enum ComponentType {
  cpu
  gpu
  ram
  ssd
  motherboard
  psu
  case
  cooler
}
